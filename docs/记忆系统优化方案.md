# 记忆系统优化方案

> 基于《Agent记忆系统构建指南》，结合项目现状（内存态游戏模拟、无向量数据库、无embedding服务）裁剪适配。

---

## 现状问题总结

| 模块 | 问题 |
|------|------|
| `memory_engine.py` | 双存储（`memory_stream` + `memory`）冗余；硬截断不考虑重要性 |
| `memory_retriever.py` | 13个硬编码关键词，覆盖面极窄；无记忆类型加权 |
| `reflection_engine.py` | 反思prompt无角色人设，所有角色风格一样；反思后不压缩旧记忆 |
| `prompt_context.py` | 只取5条记忆，硬截断丢弃重要上下文 |
| `pantry_engine.py` | 茶歇记忆kind误标为"meeting" |
| 全局 | 无去重机制、无遗忘衰减、无角色间关系追踪 |

---

## 阶段一：低成本高收益（纯逻辑改动，不增加LLM调用）

### 1.1 修复茶歇记忆kind错误

**文件**: `pantry_engine.py:99`

```python
# 修改前
kind="meeting"
# 修改后
kind="pantry"
```

---

### 1.2 统一记忆存储，去掉冗余memory字段

**现状**: `ActorRuntimeState` 同时维护 `memory_stream`（80条MemoryRecord）和 `memory`（40条纯文本），`memory` 仅在 `step_engine.py:167` 用于对话生成snapshot。

**文件**: `runtime_state.py`, `memory_engine.py`, `step_engine.py`

**改动**:
- `runtime_state.py`: 删除 `memory` 字段，新增 `memory_texts` 属性
- `memory_engine.py`: 移除 `actor.memory.append()` 和 `actor.memory = actor.memory[-40:]` 逻辑
- `step_engine.py`: 将 `actor.memory[-5:]` 替换为 `actor.memory_texts[-5:]`

```python
# runtime_state.py - ActorRuntimeState 中
# 删除: memory: list[str] = field(default_factory=list)
# 新增:
@property
def memory_texts(self) -> list[str]:
    return [m.text for m in self.memory_stream]
```

```python
# memory_engine.py - append_actor_memory 中
# 删除这两行:
#   actor.memory.append(clean_text)
#   actor.memory = actor.memory[-40:]
```

```python
# step_engine.py:167
# 修改前
"memory": actor.memory[-5:]
# 修改后
"memory": actor.memory_texts[-5:]
```

---

### 1.3 基于重要性的淘汰策略

**现状**: `memory_engine.py:209` 直接 `[-80:]` 截断，不考虑importance。

**文件**: `memory_engine.py`

**改动**: 将硬截断替换为重要性淘汰，保留最近N条不参与淘汰。

```python
MAX_MEMORY_STREAM = 120
PROTECTED_RECENT = 10  # 最近10条不参与淘汰

def _evict_lowest_importance(memory_stream: list[MemoryRecord]) -> list[MemoryRecord]:
    if len(memory_stream) <= MAX_MEMORY_STREAM:
        return memory_stream
    protected = memory_stream[-PROTECTED_RECENT:]
    candidates = memory_stream[:-PROTECTED_RECENT]
    to_remove = len(memory_stream) - MAX_MEMORY_STREAM
    candidates.sort(key=lambda m: m.importance)
    survivors = candidates[to_remove:]
    return sorted(protected + survivors, key=lambda m: m.memory_id)
```

```python
# append_actor_memory 中替换:
# 修改前
actor.memory_stream.append(record)
actor.memory_stream = actor.memory_stream[-80:]
# 修改后
actor.memory_stream.append(record)
actor.memory_stream = _evict_lowest_importance(actor.memory_stream)
```

---

### 1.4 检索器增强：扩展关键词 + 类型加权

**现状**: `memory_retriever.py` 只有13个硬编码关键词。

**文件**: `memory_retriever.py`

**改动**:

```python
# 扩展关键词列表
IMPORTANT_TERMS = [
    # 原有
    "现金", "客户", "竞对", "挖走", "离职", "预算", "延期",
    "Demo", "传播", "技术", "会议", "成本", "需求",
    # 新增 - 角色关系
    "信任", "怀疑", "支持", "反对", "冲突", "合作", "背刺",
    # 新增 - 情绪/压力
    "焦虑", "崩溃", "疲惫", "满意", "不满", "抱怨",
    # 新增 - 决策/行动
    "拍板", "妥协", "让步", "坚持", "拒绝", "同意", "投票",
    # 新增 - 风险/机会
    "风险", "机会", "危机", "转机", "救命", "完蛋",
]

# 类型权重
KIND_WEIGHT = {
    "reflection": 1.3,
    "meeting": 1.2,
    "user_input": 1.15,
    "incident": 1.25,
    "pantry": 1.1,
    "observation": 1.0,
}

# retrieve_relevant_memories 中应用
def retrieve_relevant_memories(
    actor: ActorRuntimeState,
    *,
    query: str,
    limit: int = 6,
) -> list[str]:
    query_terms = _extract_terms(query)
    scored: list[tuple[float, MemoryRecord]] = []
    total = len(actor.memory_stream)

    for index, memory in enumerate(actor.memory_stream):
        recency_score = index / max(1, total)
        importance_score = memory.importance / 10
        relevance_score = _keyword_score(memory.text, query_terms)
        kind_weight = KIND_WEIGHT.get(memory.kind, 1.0)

        # archived 记忆降权（阶段二实现，此处预留）
        archived_factor = 0.3 if memory.archived else 1.0

        score = (
            (recency_score * 0.2 + importance_score * 0.3 + relevance_score * 0.35)
            * kind_weight
            * archived_factor
        )
        scored.append((score, memory))

    scored.sort(key=lambda item: item[0], reverse=True)
    return [memory.text for _, memory in scored[:limit]]
```

---

### 1.5 prompt_context记忆条数从5提升到8

**文件**: `prompt_context.py:58`

```python
# 修改前
memory_text = "\n".join(f"- {item}" for item in (memories or [])[-5:])
# 修改后
memory_text = "\n".join(f"- {item}" for item in (memories or [])[-8:])
```

---

## 阶段二：语义增强（需少量LLM调用，可异步）

### 2.1 MemoryRecord新增archived字段

**文件**: `runtime_state.py`

```python
@dataclass
class MemoryRecord:
    memory_id: int
    kind: str
    text: str
    day: int
    step: int
    clock: str
    importance: int = 1
    tags: list[str] = field(default_factory=list)
    related_actor_ids: list[str] = field(default_factory=list)
    archived: bool = False  # 新增：被反思覆盖后标记为True
```

---

### 2.2 反思引擎加入角色人设

**文件**: `reflection_engine.py`

**改动**: `_build_reflections` 接收角色信息，让反思个性化。

```python
def schedule_memory_reflections(
    world: WorldRuntimeState,
    *,
    world_lock: threading.Lock,
) -> None:
    for actor in world.actors.values():
        if actor.reflection_importance_buffer < REFLECTION_THRESHOLD:
            continue

        actor_id = actor.actor_id
        with _RUNNING_LOCK:
            if actor_id in _RUNNING_ACTOR_IDS:
                continue
            _RUNNING_ACTOR_IDS.add(actor_id)

        memory_snapshot = list(actor.memory_stream[-REFLECTION_MEMORY_LIMIT:])
        display_name = actor.display_name

        # 新增：获取角色配置
        from src.core.world.seed_loader import load_world_seed
        seed = load_world_seed()
        character_by_id = {item["actor_id"]: item for item in seed["characters"]}
        character = character_by_id.get(actor_id)

        _REFLECTION_EXECUTOR.submit(
            _run_actor_reflection_task,
            world, world_lock, actor_id, display_name,
            memory_snapshot, character,  # 新增
        )


def _build_reflections(
    *,
    display_name: str,
    memories: list[MemoryRecord],
    character: dict[str, Any] | None = None,  # 新增
) -> list[str]:
    if not memories:
        return []

    personality_hint = ""
    if character:
        profile = character.get("character_profile", {}) or {}
        personality = profile.get("personality", {}) or {}
        style = personality.get("speaking_style", "")
        shadow = personality.get("shadow_pattern", "")
        job = character.get("job_profile", {}).get("role_name", "")
        personality_hint = (
            f"你是{job}，说话风格：{style}，"
            f"内心阴影：{shadow}。\n"
        )

    memory_text = "\n".join(
        f"- {memory.clock} [{memory.kind}] {memory.text}"
        for memory in memories
    )

    messages = [
        {
            "role": "system",
            "content": (
                f"你是职场模拟中的{display_name}。{personality_hint}"
                "请根据最近的记忆，用你自己的语气提炼你形成的稳定判断。"
                "只输出 JSON，不要 Markdown，不要解释。"
                '{"reflections":["反思1","反思2","反思3"]}'
            ),
        },
        {
            "role": "user",
            "content": (
                f"最近记忆：\n{memory_text}\n\n"
                "请生成 2 到 3 条短反思。"
                "反思要像你自己的内心独白，不要像工作总结。"
                "优先关注：用户说的话、突发事件、同事态度、公司风险、自己的压力。"
            ),
        },
    ]
    # ... 后续不变
```

---

### 2.3 反思后压缩旧记忆

**文件**: `reflection_engine.py`

**改动**: 反思完成后，将被反思的原始记忆标记为archived。

```python
def _run_actor_reflection_task(
    world, world_lock, actor_id, display_name,
    memory_snapshot, character,
) -> None:
    try:
        reflections = _build_reflections(
            display_name=display_name,
            memories=memory_snapshot,
            character=character,
        )
        if not reflections:
            return

        with world_lock:
            actor = world.actors.get(actor_id)
            if actor is None:
                return

            for reflection in reflections:
                append_actor_memory(
                    world,
                    actor_id=actor_id,
                    kind="reflection",
                    text=reflection,
                    clock=world.company.clock,
                    tags=["reflection"],
                )

            # 新增：标记被反思的原始记忆为archived
            for memory in memory_snapshot:
                memory.archived = True

            actor.reflection_importance_buffer = 0
    finally:
        with _RUNNING_LOCK:
            _RUNNING_ACTOR_IDS.discard(actor_id)
```

---

### 2.4 记忆去重（纯字符串匹配，不需LLM）

**文件**: `memory_engine.py`

**改动**: 写入新记忆前，检查是否与最近记忆重复。

```python
DEDUP_WINDOW = 20
DEDUP_THRESHOLD = 0.65  # 共享词比例阈值

def _is_duplicate(new_text: str, existing: list[MemoryRecord], window: int = DEDUP_WINDOW) -> bool:
    new_chars = set(new_text)
    for old in existing[-window:]:
        old_chars = set(old.text)
        overlap = len(new_chars & old_chars) / max(len(new_chars | old_chars), 1)
        if overlap > DEDUP_THRESHOLD:
            return True
    return False


def append_actor_memory(world, *, actor_id, kind, text, clock, related_actor_ids=None, tags=None):
    actor = world.actors.get(actor_id)
    if actor is None:
        return

    clean_text = text.strip()
    if not clean_text:
        return

    # 新增：去重检查（reflection和meeting不参与去重）
    if kind not in ("reflection", "meeting") and _is_duplicate(clean_text, actor.memory_stream):
        return

    importance = estimate_importance(clean_text, kind)
    record = MemoryRecord(
        memory_id=actor.memory_next_id,
        kind=kind,
        text=clean_text,
        day=world.company.day,
        step=world.company.step,
        clock=clock,
        importance=importance,
        tags=tags or [],
        related_actor_ids=related_actor_ids or [],
    )
    actor.memory_next_id += 1
    actor.memory_stream.append(record)
    actor.memory_stream = _evict_lowest_importance(actor.memory_stream)
    actor.reflection_importance_buffer += importance
```

---

## 阶段三：关系图谱（增量建设，长期价值最高）

### 3.1 Actor间关系系统

**文件**: `runtime_state.py`, `memory_engine.py`

**改动**: 新增 `RelationshipRecord`，在记忆写入时同步更新角色间关系。

```python
# runtime_state.py 新增
@dataclass
class RelationshipRecord:
    target_actor_id: str
    trust: int = 50           # 信任度 0-100
    impression: str = ""      # 最近印象摘要
    interaction_count: int = 0
    last_interaction_step: int = 0
    last_interaction_clock: str = ""

# ActorRuntimeState 新增
relationships: dict[str, RelationshipRecord] = field(default_factory=dict)
```

```python
# memory_engine.py - write_step_memories 中，观察到他人行为时更新关系
def _update_relationship(
    observer: ActorRuntimeState,
    target_actor_id: str,
    speech: str,
    step: int,
    clock: str,
    is_positive: bool = True,
) -> None:
    if target_actor_id not in observer.relationships:
        observer.relationships[target_actor_id] = RelationshipRecord(
            target_actor_id=target_actor_id
        )
    rel = observer.relationships[target_actor_id]
    rel.interaction_count += 1
    rel.last_interaction_step = step
    rel.last_interaction_clock = clock

    if is_positive:
        rel.trust = min(100, rel.trust + 3)
    else:
        rel.trust = max(0, rel.trust - 5)

    # 更新印象（取最近发言的前30字）
    if speech:
        rel.impression = speech[:30]
```

```python
# memory_retriever.py - 检索时加入关系权重
def retrieve_relevant_memories(
    actor: ActorRuntimeState,
    *,
    query: str,
    limit: int = 6,
) -> list[str]:
    query_terms = _extract_terms(query)
    scored: list[tuple[float, MemoryRecord]] = []
    total = len(actor.memory_stream)

    for index, memory in enumerate(actor.memory_stream):
        recency_score = index / max(1, total)
        importance_score = memory.importance / 10
        relevance_score = _keyword_score(memory.text, query_terms)
        kind_weight = KIND_WEIGHT.get(memory.kind, 1.0)
        archived_factor = 0.3 if memory.archived else 1.0

        # 新增：关系权重
        relationship_factor = 1.0
        if memory.related_actor_ids:
            trust_scores = []
            for rid in memory.related_actor_ids:
                rel = actor.relationships.get(rid)
                if rel:
                    trust_scores.append(rel.trust / 50)  # 0-2 的系数
            if trust_scores:
                relationship_factor = sum(trust_scores) / len(trust_scores)

        score = (
            (recency_score * 0.2 + importance_score * 0.3 + relevance_score * 0.35)
            * kind_weight
            * archived_factor
            * relationship_factor
        )
        scored.append((score, memory))

    scored.sort(key=lambda item: item[0], reverse=True)
    return [memory.text for _, memory in scored[:limit]]
```

---

### 3.2 因果链追踪（简化版）

**文件**: `runtime_state.py`, `reflection_engine.py`

**改动**: MemoryRecord 新增 `caused_by_id`，反思时让LLM识别因果关系。

```python
# runtime_state.py - MemoryRecord 新增
caused_by_id: int | None = None  # 指向导致此记忆的事件ID
```

```python
# reflection_engine.py - _build_reflections prompt 中增加因果识别
"请同时指出，哪些反思是由哪些具体事件直接导致的。"
'格式：{"reflections":[{"text":"反思内容","caused_by_ids":[事件memory_id]}]}'
```

```python
# memory_retriever.py - WHY类查询时沿因果链回溯
def retrieve_causal_chain(
    actor: ActorRuntimeState,
    target_memory_id: int,
    max_depth: int = 3,
) -> list[str]:
    """沿因果链回溯，返回导致target_memory的因果链"""
    chain = []
    current_id = target_memory_id
    id_to_memory = {m.memory_id: m for m in actor.memory_stream}

    for _ in range(max_depth):
        memory = id_to_memory.get(current_id)
        if memory is None:
            break
        chain.append(memory.text)
        if memory.caused_by_id is None:
            break
        current_id = memory.caused_by_id

    return list(reversed(chain))  # 从因到果
```

---

## 优先级与工作量

| 顺序 | 改动 | 涉及文件 | 代码量 | 收益 |
|------|------|----------|--------|------|
| 1 | 修复pantry kind | `pantry_engine.py` | 1行 | 修复bug |
| 2 | 重要性淘汰 | `memory_engine.py` | ~30行 | 记忆质量提升 |
| 3 | 统一存储 | `runtime_state.py` `memory_engine.py` `step_engine.py` | ~20行 | 减少混乱 |
| 4 | 检索器增强 | `memory_retriever.py` | ~40行 | 检索准确度 |
| 5 | prompt记忆条数 | `prompt_context.py` | 1行 | 上下文丰富 |
| 6 | MemoryRecord archived | `runtime_state.py` | 1行 | 基础设施 |
| 7 | 反思角色人设 | `reflection_engine.py` | ~30行 | 个性化 |
| 8 | 反思后压缩 | `reflection_engine.py` | ~10行 | 减少冗余 |
| 9 | 记忆去重 | `memory_engine.py` | ~30行 | 减少噪声 |
| 10 | Actor关系系统 | `runtime_state.py` `memory_engine.py` `memory_retriever.py` | ~80行 | 长期价值最高 |
| 11 | 因果链追踪 | `runtime_state.py` `reflection_engine.py` `memory_retriever.py` | ~50行 | WHY查询 |

---

## 与指南论文的对应关系

| 本方案改动 | 对应论文/机制 | 适配说明 |
|------------|--------------|----------|
| 重要性淘汰 | GAM - 层次化图记忆 | 简化为单层importance淘汰，不做多层归档 |
| 类型加权检索 | MAGMA - 意图感知检索 | 用kind权重替代完整的意图分类器 |
| 记忆去重 | Mem0 - UPDATE/NOOP | 用字符串相似度替代LLM决策，零额外成本 |
| 反思压缩 | GAM - 语义转折触发整合 | 反思即"转折点"，archived即"归档" |
| Actor关系 | MAGMA - 实体图 | 简化为trust+impression，不做四维正交图 |
| 因果链 | MAGMA - 因果图 | 仅在反思时识别，不做实时因果推断 |
| 角色人设反思 | MemMachine - 上下文化 | 将角色profile注入反思prompt |
